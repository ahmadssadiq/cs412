{
    "builds": [
        {
            "src": "cs412/wsgi.py",
            "use": "@vercel/python"
        }
    ],
    "buildCommand": "pip install -r ./requirements.txt && python manage.py collectstatic --noinput",
    "routes": [
        {   
            "src": "/static/(.*)",
            "dest": "/static/$1"
        },
        {
            "src": "/(.*)",
            "dest": "cs412/wsgi.py"
        }
    ]
}